groups:
  - name: rag_alerts
    rules:

      - alert: HighErrorRate
        expr: increase(rag_request_errors_total[1m]) > 5
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "High error rate in RAG service"
          description: "More than 5 errors occurred in the last minute."